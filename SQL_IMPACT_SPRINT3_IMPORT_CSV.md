# Sprint 3 — Import CSV (Impact SQL)

## Impact schéma
Aucune modification de schéma n'est nécessaire pour ce sprint.

## Tables utilisées
- `prospects` (INSERT)

## Requête d'insertion utilisée
L'import CSV réutilise la méthode existante `ProspectModel::create()` qui exécute un `INSERT INTO prospects (...) VALUES (...)`.
